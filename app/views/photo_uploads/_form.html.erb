<%= simple_form_for(@photo_upload) do |f| %>
  <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :image %>
    </div>

    <div class="form-actions">
      <%= f.button :submit %>
    </div>
    <% end %>